<nav class="navbar navbar-default navbar-fixed-top main-navbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Project: Blog", root_path, class:"navbar-brand", id:"logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'v1.0.0', release_notes_path, id:"version" %></li>
        <li><%= link_to "Users", users_path %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Projects <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "My Projects", user_projects_path(current_user) %></li>
              <li><%= link_to "Create a new Project", new_project_path %></li>
            </ul>
          </li>
          <li><%= link_to "My Profile", user_profile_path(current_user) %></li>
          <li><%= link_to "My Account", edit_user_registration_path %></li>
        <% elsif admin_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Languages <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Languages", languages_path %></li>
              <li><%= link_to "Create a new Language", new_language_path %></li>
            </ul>
          </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="user-name text-right">
          <%= icon('fas', 'user') %> 
          <% if user_signed_in? %>
            <%= current_user.user_name %>
          <% elsif admin_signed_in? %>
            Admin
          <% end %>
        </li>
        <li>
          <% if user_signed_in? || admin_signed_in? %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete %>
          <% else %>
            <%= link_to "Login", new_user_session_path %>
          <% end %>
        </li>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>